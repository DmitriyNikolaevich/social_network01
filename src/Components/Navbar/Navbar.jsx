import React from 'react';
import s from './Navbar.module.css';

const Navbar = (props) => {
    return (
        <div className={s.wrapper}>
            Navbar
        </div>
    )
}

export default Navbar;