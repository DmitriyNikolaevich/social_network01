import React from 'react';
import s from './Variant.module.css';

const Variant = (props) => {
    return (
        <div>
            <button className={s.button}>Variant name</button>
        </div>
    )
}

export default Variant;